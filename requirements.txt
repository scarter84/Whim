# Whim Terminal — Core Dependencies
# Install: pip install -r requirements.txt

Pillow>=10.0
pystray>=0.19
qrcode[pil]>=7.4
sounddevice>=0.4
numpy>=1.24
websockets>=12.0
aiohttp>=3.9

# Optional: Screen capture
mss>=9.0
